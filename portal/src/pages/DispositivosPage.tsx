import { PagePlaceholder } from "@/components/PagePlaceholder";

export function DispositivosPage() {
  return (
    <PagePlaceholder
      title="Dispositivos"
      description="Inventário e saúde dos agentes: último contato, versão do agente, fuso da máquina, tags e status (ativo, pausado, arquivado, revogado)."
      phase="F2"
    />
  );
}
